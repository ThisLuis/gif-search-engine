import React from 'react'
import ReactDOM from 'react-dom/client'
import { GifSearchEngine } from './GifSearchEngine';

import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GifSearchEngine />
  </React.StrictMode>,
)
